# Ajoutez cette ligne
custom:
  test-value: "hello-from-config-server-123"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.j2ee.profils: DEBUG
